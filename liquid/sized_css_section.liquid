{% render 'overlays_css_snippet', config:config, size:size, overlays:overlays %}
{% render 'logo_css_snippet', page:page, config:config, size:size %}
{% render 'types_css_section', config:config, page:page, size:size %}
{% if page.content %}
  {% render 't_content_css', widths:config.widths,
                             colours:config.colours,
                             content:page.content,
                             dimensions:config.dimensions,
                             size:size %}
{% endif %}
{% if size == 1 %}
  {% unless page.text_index %}
    .text_indexes {display: none}
  {% endunless %}
{% endif %}
{% render 'image_indexes_css_snippet', colours:config.colours,
                                       image_index:page.image_index,
                                       size:size %}
{% render 'rect_indexes_css_snippet', config:config,
                                      rect_index:page.rect_index,
                                      size:size %}

