{% if page.page_title %}
{% if page.previous_page or page.next_page %}
a.nav {display: flex; align-items: center}
.nav_rect      {height: 6px; width: 14px; background: {{ colours.anchor }};
                display: inline-block}
.nav_rtriangle {height: 0px; width: 0px;
                border-left: 10px solid {{ colours.anchor }};
                border-top: 10px solid transparent;
                border-bottom: 10px solid transparent;
                display: inline-block}
.nav_ltriangle {height: 0px; width: 0px;
                border-right: 10px solid {{ colours.anchor }};
                border-top: 10px solid transparent;
                border-bottom: 10px solid transparent;
                display: inline-block}
.nav_none      {width: 24px}
a:hover .nav_rect {background: {{ colours.hover }} }
a:hover .nav_ltriangle {border-right-color: {{ colours.hover }} }
a:hover .nav_rtriangle {border-left-color: {{ colours.hover }} }

@media all and (min-width: {{ widths.browser[1] }}px) {
  .nav_rect {height: 9px; width: 21px}
  .nav_ltriangle {border-right-width: 15px;
                  border-top-width: 15px;
                  border-bottom-width: 15px}
  .nav_rtriangle {border-left-width: 15px;
                  border-top-width: 15px;
                  border-bottom-width: 15px}
  .nav_none {width: 36px}
}
{% endif %}
{% endif %}

