{% if page.content %}
  {% assign types = page.content | map: "type" | uniq %}
  {% for type in types %}
    {% case type %}
    {% when 'code' %}
      {% render 'code_css_snippet', code:element, size:size %}
    {% endcase %}
  {% endfor %}
{% endif %}

