{% if details %}
<IMG {% if index %}ID="I{{ index }}"{% endif %} {% render 't_clazz' with clazz:clazz %} SRC="{{ details.path }}"
  {% if title %} TITLE="{{ title }}"{% endif %}
   ALT="{{ details.tag }}"
{% if overlay %}
  onclick="javascript: showOverlay( '{{ overlay }}');"
{% endif %}
>
{% endif %}
