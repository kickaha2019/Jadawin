<TABLE CLASS="table">
  {% for row in table.rows %}
    <TR>
      {% for cell in row %}
        {% if cell.bold %}
          <TD STYLE="font-weight: bold; text-align: {{ cell.align }}">
        {% else %}
          <TD STYLE="text-align: {{ cell.align }}">
        {% endif %}
        {% render 'markup_section' with markup:cell.data %}
        </TD>
      {% endfor %}
    </TR>
  {% endfor %}
</TABLE>
